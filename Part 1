touch .bash_profile
echo 'export PS1="\e[0;32m \w \e[m$"
			export PATH=$PATH:.
			alias dir="ls -l"
			alias cls="clear"
			w
			alias rm="rm -i"
			set -o noclobber
			##could not figure out what you meant by the bold thing' >> .bash_profile
cat .bash_profile
